=== SCOAP3 Collections Status ===
Contributors: wziolek

Adds a short code to display a dynamically updated table showing current articles count for different SCOAP3 collections

== Description ==

Adds a short code to display a dynamically updated table showing current articles count for different SCOAP3 collections. It is using SCOAP3 "API" that returns a JSONP object. URL to use: https://repo.scoap3.org/tools.py/get_collections_count?callback=?.
